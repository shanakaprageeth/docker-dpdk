Note: Please edit rhel subscription manager USERNAME and PASSWORD or else set up yum in DOCKERFILE.
additionally, dpdk source configuration works but build fails.
## docker ubuntu dpdk
To execute the prebuild container
```
./install-image-with-docker.sh
```

To build and execute the container
```
./install-image-with-docker.sh build
```

Build location ~/dpdk/build